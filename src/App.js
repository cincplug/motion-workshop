// Libs
import React from 'react';
// import PropTypes from 'prop-types';

// Utils

// Components

// Component
const App = () => {
  return <main>
    <h1>Motion Workshop</h1>
  </main>;
};

// Props
App.defaultProps = {
};

// const { } = PropTypes;

App.propTypes = {
};

export default App;
